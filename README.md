# stasis-hunters

A narrative-driven game about memories, choices, and consequences.

## Running the Game

To run the game, execute the following command from the root directory of the project:

```bash
python -m src.game
```

## Running Tests

To run the test suite, use the following command from the root directory:

```bash
python -m unittest discover src/tests
```